// packages/mobile/tests/setup.ts
// Global vitest setup — see vitest.config.ts's `setupFiles`.
import { afterEach } from "vitest";

afterEach(() => {
  // no shared state yet — placeholder for when React Testing Library
  // cleanup is needed on UI tests.
});
