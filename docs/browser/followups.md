# Browser v2 — tracked follow-ups

Items deferred from a shipped sub-phase. Each entry: who deferred it, when,
why, target window, and the acceptance criteria if it were to land.

| Id      | Item                                                                                                     | Deferred in | Target window                | Why deferred                                                                                                  | Acceptance                                                                                           |
| ------- | -------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| BFW-001 | Decrypt Chrome OS-keyring-encrypted cookies on import (DPAPI / Keychain / libsecret)                     | G1-a        | G1-e or dedicated sub-phase  | Three OS-specific bridges; same-user-session guarantees; out of G1-a scope                                    | Import flow + unit tests per OS + docs update removing the cookies-not-readable caveat               |
| BFW-002 | OS-keyring integration for profile passphrase (macOS Keychain / Windows Credential Manager / libsecret)  | G1-a        | **shipped in post-G9 follow-up** | Shell-out to `secret-tool`/`security`/`cmdkey`+PowerShell — no native binding. `createDefaultKeyring()` + `profilePassphraseEntry()` + `HIPP0_KEYRING_BACKEND=memory` override for CI | ✅ Keyring round-trip tests + 4 backends + platform auto-select                                        |
| BFW-003 | Video / audio reasoning (watch tutorial video, extract steps)                                            | G1 plan     | Post-G1 multimodal phase     | Multimodal-LLM capability, not a browser one; belongs near `core/llm` with a vision adapter                   | Separate design doc first                                                                            |
| BFW-004 | "Bypass UI via API" planner behavior (agent detects a slow UI is a thin wrapper and calls the API)       | G1 plan     | Future agent/orchestrator phase | Agent-planner decision, not a browser capability                                                              | Separate design doc first; G1-e ships the network-observability substrate it'd depend on             |
| BFW-005 | Mobile package `tsc --noEmit` failures (React 19 + RN / lucide-react / expo-camera type drift, 12 errors) | pre-existing on `feat/phase-20-eval`, surfaced during G1-a gate | Dedicated mobile-hygiene sub-phase | Upstream RN + lucide-react haven't released React 19 compatible types yet; not a G1 scope item               | `pnpm --filter @openhipp0/mobile build` exits 0; no `@ts-ignore` or `as unknown as` hacks            |
| BFW-006 | Camoufox evaluation against pinned stealth fixture set | G1-e                                                              | Post-G1 if fixture gap warrants engine switch | Requires separate binary (~200 MB) + engine-switch affects every `BrowserDriver` consumer                  | Camoufox fixture score vs. Playwright-extra baseline documented; decision recorded as DECISION entry |
| BFW-007 | Real `playwright-extra` + stealth-plugin adapter wiring                                                 | G1-e                                                              | When CLI adds `--stealth` flag       | G1-e ships descriptors + addInitScript; production glue can land alongside the CLI surface                 | `hipp0 browser stealth doctor` works against live browser                                           |
| BFW-008 | curl-impersonate ja3 TLS sidecar adapter                                                                | G1-e                                                              | Opt-in deployment-time option        | Requires external binary + DNS/proxy config; documented as deployment choice                                | `HIPP0_BROWSER_TLS_PROXY` doc + integration test with a mock curl-impersonate proxy                |

To add a new entry: append a row with the next `BFW-NNN` id. Include the
sub-phase that deferred it so we can trace back to the decision log.
